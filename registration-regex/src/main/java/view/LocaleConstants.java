package view;

import java.util.Locale;

public interface LocaleConstants {
    Locale EN = new Locale("en", "EN");
    Locale UA = new Locale("ua", "UA");
    Locale RU = new Locale("ru", "RU");

    Locale APP_LOCALE = RU;
}
